{
  "name": "rubberband-web",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "https://github.com/delude88/rubberband-web.git",
  "author": "delude88 <tobias.hegemann@googlemail.com>",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "dev": "tsc --watch",
    "dev:example": "tsc -b example --watch",
    "build": "npm run build:wasm && npm run build:ts && npm run build:example",
    "build:wasm": "./build.sh",
    "build:ts": "tsc",
    "build:example": "tsc -b example",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "@types/audioworklet": "^0.0.33",
    "@types/emscripten": "^1.39.6",
    "@types/jest": "^29.0.3",
    "cross-env": "^7.0.3",
    "jest": "^29.0.3",
    "ts-jest": "^29.0.1",
    "typescript": "^4.8.3"
  }
}
